# Reproducibility data

Download the `v0.1.0` data archives and extract them at the repository root. The original WOS exports and all selected computational inputs will populate this directory without anonymization. See the root `DATA_AVAILABILITY.md` for paths and reuse considerations.
